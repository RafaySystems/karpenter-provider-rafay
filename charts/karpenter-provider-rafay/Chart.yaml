apiVersion: v2
name: karpenter-provider-rafay
description: Helm chart for the Rafay Karpenter cloud provider (private cloud / edge-broker).
type: application
version: 0.2.0
# Controller binary version (image tag); chart 0.2.0 adds optional pools templates.
appVersion: "0.1.0"
