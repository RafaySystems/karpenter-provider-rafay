sonar.projectKey=karpenter-provider-rafay
sonar.sources=.
# Scanner-side exclusions replace the server-side list, so the standard Rafay patterns are repeated here.
# vendor/ is go mod vendor output (git-ignored, but excluded in case a checkout carries one).
sonar.exclusions=**/*Dockerfile*,**rafaydocs**/*,**/*mocks*,**/*test*/**,**/*yaml*,**/**/*.js,rafay-docs/**,**/*yml*,certs/**,vendor/**
